const ICONS={'Gaming Keyboards':'⌨️','Gaming Mice':'🖱️','Gaming Mouse':'🖱️','Gaming Headsets':'🎧','Gaming Controllers':'🎮','Gaming Monitors':'🖥️','Gaming Chairs':'🪑','Gaming Consoles':'🕹️','Gaming Microphones':'🎙️','Gaming VR Headsets':'🥽','Gaming Mouse Pads':'🖱️','Graphics Card':'💻','Cooling System':'❄️','RGB Lights Kits':'💡','Streaming Gears':'📡','Anti-Fatigue Mats':'🧘','Monitor Stands':'🖥️'};
const icon=n=>ICONS[n]||'🎮';
let cart=JSON.parse(localStorage.getItem('gs_cart')||'[]');
function save(){localStorage.setItem('gs_cart',JSON.stringify(cart));}
function addToCart(name){const ex=cart.find(i=>i.name===name);if(ex)ex.qty++;else cart.push({name,qty:1});save();updateUI();toast(name);}
function removeItem(name){cart=cart.filter(i=>i.name!==name);save();updateUI();}
function changeQty(name,d){const it=cart.find(i=>i.name===name);if(!it)return;it.qty+=d;if(it.qty<=0)removeItem(name);else{save();updateUI();}}
function clearCart(){cart=[];save();updateUI();}
function totalQty(){return cart.reduce((s,i)=>s+i.qty,0);}
function updateUI(){updateBadge();renderItems();syncSummary();}
function updateBadge(){const t=totalQty();document.querySelectorAll('.cart-badge').forEach(b=>{b.textContent=t;b.classList.toggle('hidden',t===0);});}
function renderItems(){const list=document.getElementById('cartItemsList');if(!list)return;if(cart.length===0){list.innerHTML='<div class="cart-empty"><div class="empty-icon">🛒</div><p>Your cart is empty.<br>Add some gaming gear!</p></div>';return;}list.innerHTML=cart.map(item=>`<div class="cart-item"><div class="cart-item-icon">${icon(item.name)}</div><div class="cart-item-info"><div class="cart-item-name">${item.name}</div><div class="cart-item-qty">Qty: ${item.qty}</div></div><div class="cart-qty-controls"><button class="qty-btn" onclick="changeQty('${item.name}',-1)">−</button><span class="qty-number">${item.qty}</span><button class="qty-btn" onclick="changeQty('${item.name}',1)">+</button></div><button class="cart-remove-btn" onclick="removeItem('${item.name}')">✕</button></div>`).join('');const sc=document.getElementById('cartSummaryCount');if(sc)sc.textContent=`${totalQty()} item${totalQty()!==1?'s':''}`;}
function syncSummary(){const c=document.getElementById('checkoutItemLines');if(!c)return;c.innerHTML=cart.map(i=>`<div class="checkout-item-line"><span>${icon(i.name)} ${i.name}</span><span>x${i.qty}</span></div>`).join('');}
function toast(name){let t=document.getElementById('gsToast');if(!t){t=document.createElement('div');t.id='gsToast';t.style.cssText='position:fixed;bottom:24px;left:50%;transform:translateX(-50%) translateY(80px);background:#ff4747;color:#fff;padding:12px 24px;border-radius:50px;font-size:0.9rem;font-weight:600;z-index:99999;transition:transform 0.35s cubic-bezier(0.34,1.56,0.64,1);box-shadow:0 6px 25px rgba(255,71,71,0.5);pointer-events:none;white-space:nowrap;';document.body.appendChild(t);}t.textContent=`✅ ${name} added to cart!`;t.style.transform='translateX(-50%) translateY(0)';clearTimeout(t._t);t._t=setTimeout(()=>{t.style.transform='translateX(-50%) translateY(80px)';},2500);}
function openCart(){document.getElementById('cartSidebar')?.classList.add('open');document.getElementById('cartOverlay')?.classList.add('open');document.body.style.overflow='hidden';renderItems();}
function closeCart(){document.getElementById('cartSidebar')?.classList.remove('open');document.getElementById('cartOverlay')?.classList.remove('open');document.body.style.overflow='';}
function openCheckout(){if(cart.length===0)return;syncSummary();closeCart();const m=document.getElementById('checkoutModal');if(m){m.classList.add('active');document.getElementById('checkoutMsg').textContent='';document.getElementById('checkoutName').value='';document.getElementById('checkoutEmail').value='';document.getElementById('checkoutPhone').value='';}}
function closeCheckout(){document.getElementById('checkoutModal')?.classList.remove('active');}
async function placeOrder(){const name=document.getElementById('checkoutName').value.trim();const email=document.getElementById('checkoutEmail').value.trim();const phone=document.getElementById('checkoutPhone').value.trim();const msg=document.getElementById('checkoutMsg');const btn=document.getElementById('checkoutSubmitBtn');if(!name||!email||!phone){msg.textContent='⚠️ Fill all fields.';msg.className='checkout-msg error';return;}if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){msg.textContent='⚠️ Valid email needed.';msg.className='checkout-msg error';return;}if(!cart.length)return;btn.disabled=true;btn.textContent='⏳ PLACING...';msg.textContent='';try{const res=await fetch(`${window.location.origin}/api/orders/place`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customerName:name,customerEmail:email,customerPhone:phone,items:cart})});const data=await res.json();if(data.success){msg.textContent='✅ Order placed! Check your email for confirmation.';msg.className='checkout-msg success';clearCart();setTimeout(closeCheckout,3500);}else{msg.textContent='❌ '+(data.errors?.join(', ')||data.message);msg.className='checkout-msg error';}}catch{msg.textContent='❌ Something went wrong. Please try again.';msg.className='checkout-msg error';}finally{btn.disabled=false;btn.textContent='CONFIRM ORDER';}}
document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('.add-cart-btn').forEach(btn=>{btn.addEventListener('click',function(){addToCart(this.dataset.product);this.textContent='✓ Added!';this.classList.add('added');setTimeout(()=>{this.textContent='+ Add to Cart';this.classList.remove('added');},1500);});});document.getElementById('cartNavBtn')?.addEventListener('click',openCart);document.getElementById('cartOverlay')?.addEventListener('click',closeCart);document.getElementById('cartCloseBtn')?.addEventListener('click',closeCart);document.getElementById('cartCheckoutBtn')?.addEventListener('click',openCheckout);document.getElementById('cartClearBtn')?.addEventListener('click',()=>{if(confirm('Clear cart?'))clearCart();});document.getElementById('checkoutCloseBtn')?.addEventListener('click',closeCheckout);document.getElementById('checkoutSubmitBtn')?.addEventListener('click',placeOrder);updateUI();});
